# Public client configuration only. Never add a Supabase service key or database password.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# Maps are Bing-only (Leaflet + Esri tiles in bing-map.html); no Google key needed.

# Used for private routing requests from the desktop client.
FREEROUTE_API_KEY=
